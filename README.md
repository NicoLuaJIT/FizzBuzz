# FizzBuzz

FizzBuzz made in Rust.

# How to Run

Download the `fizzbuzz.rs` file and then open up your terminal. Then run this command `rustc fizzbuzz.rs`, and then execute the executable by running these OS-specific commands:

### Windows

`.\fizzbuzz`

### macOS or Linux

`./fizzbuzz`
